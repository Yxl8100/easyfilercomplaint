---
phase: 9
slug: complaint-narrative-engine-ag-pdf-success-page
status: draft
shadcn_initialized: true
preset: base-nova / neutral / cssVariables
created: 2026-05-03
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for the success page redesign at `/filing/[id]/success`.
> The only frontend UI deliverable in Phase 9. Backend modules (cppa-complaint-generator.ts,
> generate-complaint-pdf.ts) have no UI surface and are not covered here.
>
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova, baseColor: neutral, cssVariables: true |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font — Heading | Fraunces (var(--font-fraunces)) — serif |
| Font — Body | Inter (var(--font-inter)) — sans-serif |
| Font — Mono | JetBrains Mono (var(--font-jetbrains)) — monospace |

Source: `components.json`, `tailwind.config.ts` (verified in session).

### Existing shadcn components available

`badge`, `button`, `card`, `dialog`, `input`, `label`, `separator`, `sonner`

No new shadcn components are required for Phase 9. All channel cards and status badges
are composed from existing `bg-bg-alt / border / rounded-[6px]` patterns already used
in the current success page.

### Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none (existing components only) | not required |

No third-party registries. No vetting gate needed.

---

## Spacing Scale

All values are multiples of 4. Sourced from existing success page patterns and Tailwind
utility classes already in use across the codebase.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Label-to-value gap within channel cards |
| md | 16px | Default element spacing, card internal padding top/bottom |
| lg | 24px | Card internal padding (p-6), channel card vertical gap |
| xl | 32px | Section-to-section gap (mb-8) |
| 2xl | 48px | Page top padding (py-12 = 48px) |
| 3xl | 64px | Not used in this phase |

Exceptions:

- Touch targets for CTA buttons: minimum 44px height. The existing `py-3.5` (14px top +
  14px bottom + ~16px line-height = ~44px) on primary buttons already satisfies this.
  Do not reduce button padding below `py-3` on any interactive element.
- Channel card gap: `mb-4` (16px) between CPPA Online, CPPA Paper PDF, and CA AG cards.
  `mb-8` (32px) between the confirmation header block and the first channel card.

---

## Typography

Sourced from existing codebase patterns in `success/page.tsx` and `Masthead.tsx`.
Consolidated to exactly 4 sizes. No new type roles introduced in Phase 9.

| Role | Size | Weight | Line Height | Font | Class Pattern |
|------|------|--------|-------------|------|---------------|
| Display | 30px (text-3xl) | 700 (font-bold) | 1.2 | Fraunces (font-serif) | `font-serif text-3xl font-bold` |
| Large / Heading | 20px (text-xl) | 700 (font-bold) | 1.2 | JetBrains Mono (font-mono) | `font-mono text-xl font-bold tracking-wider` |
| Body | 14px (text-sm) | 400 (font-body default) | 1.5 | Inter (font-body) | `font-body text-sm` |
| Micro | 11px (text-[11px]) | 400 | 1 | JetBrains Mono (font-mono) | `font-mono text-[11px] uppercase tracking-[0.1em]` |

Role assignments:

- **Display (30px)** — h1 "Complaint Filed" on the confirmation header.
- **Large / Heading (20px)** — Receipt ID value display; channel card `<h2>` headings
  (e.g. "File your complaint online at cppa.ca.gov", "Download and mail your complaint
  form", "Your complaint was automatically submitted by fax"). Uses Fraunces serif for
  card headings: `font-serif text-xl font-bold`.
- **Body (14px)** — Body copy within channel cards, fax status text, link text in ghost
  style, filing detail row values.
- **Micro (11px)** — All of: step eyebrow labels ("STEP 1", "STEP 2", "STEP 3"),
  status badges ("Recommended", "Auto-Filed ✓", "Paid", fax status inline badge),
  CTA button text ("File Now — Step-by-Step Guide →", "Create Free Account →"),
  Fax ID row label, PDF download link text. Eyebrow variant adds `tracking-[0.15em]`
  for step labels; badge and button variants use `tracking-[0.1em]`.

Rules:
- Only the two declared weights are used: 400 (regular) and 700 (bold). Weight 500 applies
  to body `<p>` globally via `globals.css` — treat as equivalent to 400 for design purposes.
- Never introduce font-medium (500) or font-semibold (600) as a new weight in this phase.
- Channel card `<h2>` headings use the Large / Heading role at 20px (`font-serif text-xl font-bold`).
- Step labels ("STEP 1", "STEP 2", "STEP 3") use the Micro role: `font-mono text-[11px]
  uppercase tracking-[0.15em] text-text-light`.

---

## Color

Sourced directly from `tailwind.config.ts` custom tokens. The project uses named semantic
tokens, not raw hex values, in className attributes.

| Role | Token | Hex Equivalent | Usage |
|------|-------|---------------|-------|
| Dominant (60%) | `bg-bg` | #ffffff | Page background, card backgrounds |
| Secondary (30%) | `bg-bg-alt` | #f5f5f3 | All channel cards, receipt card, detail card |
| Accent — brand (10%) | `text-accent` / `bg-bg-dark` | #b91c1c / #1c1c1c | Specific elements only — see below |
| Success green | `text-accent-green` | #15803d | "Delivered" fax status badge ONLY |
| Destructive | `text-accent` (red) | #b91c1c | "Delivery Failed" fax status badge ONLY |

Accent (`text-accent` — red #b91c1c) reserved for:
- Brand wordmark "Complaint" span in Masthead (existing, not changed)
- "Delivery Failed" fax status text within the CA AG channel card

`bg-bg-dark` (#1c1c1c) reserved for:
- Primary CTA button fill ("Create Free Account" guest CTA)
- "Paid" badge on receipt card
- "Recommended" badge on CPPA Online channel card (uses `bg-bg-dark text-white`)

`accent-green` (#15803d) reserved for:
- "Delivered" fax status text within the CA AG channel card
- The confirmation checkmark icon aria-hidden display element (existing)

All borders use `border-border` (oklch(0.922 0 0) ~= #e5e5e5).
Secondary borders use `border-border-dark` (#c5c5c3) for the receipt ID card outline.

No dark mode variants are introduced in this phase. Dark mode CSS variables exist in
globals.css but Phase 9 does not add new dark-mode-specific overrides.

---

## Component Inventory — Success Page Sections

The rewritten success page has 7 sections in order. All sections are server-rendered
(no client components needed).

### Section 1: Confirmation Header (unchanged)

Preserve exactly: `✓` glyph at `text-6xl text-border-dark`, `<DoubleRule />`, h1
"Complaint Filed" at `font-serif text-3xl font-bold`. Update the subheading copy only
(see Copywriting Contract below).

### Section 2: Receipt ID Card (unchanged)

Preserve exactly. No layout or style changes.

### Section 3: Filing Detail Rows (unchanged)

Preserve exactly. Update "Agency" row value from "CA Attorney General" to "Multiple
Agencies" when `!isADA`, or "CA Attorney General" when `isADA`.

### Section 4: Three-Channel Filing Cards

Three stacked cards, each `bg-bg-alt border border-border rounded-[6px] p-6 mb-4`.

**Channel Card A — CPPA Online (hidden for ADA)**

```
[Eyebrow]  STEP 1 — CPPA Online                          [Badge: Recommended]
[Heading]  File your complaint online at cppa.ca.gov
[Body]     We've prepared your answers. Open our step-by-step guide,
           then paste each answer into the CPPA form.
[Link]     File Now — Step-by-Step Guide →
```

- Eyebrow: `font-mono text-[11px] uppercase tracking-[0.15em] text-text-light`
- "Recommended" badge: `bg-bg-dark text-white font-mono text-[11px] uppercase px-2 py-0.5 rounded-[4px]`
- Heading: `font-serif text-xl font-bold text-text`
- Body: `font-body text-sm text-text-mid`
- Link: `font-mono text-[11px] uppercase tracking-[0.1em] bg-bg-dark text-white px-6 py-3 rounded-[6px] inline-block hover:bg-text-mid transition-colors`
- href: `/filing/${filing.id}/cppa-guide`
- Condition: rendered only when `filing.category !== 'accessibility'`

**Channel Card B — CPPA Paper PDF (hidden for ADA)**

```
[Eyebrow]  STEP 2 — CPPA Paper Mail
[Heading]  Download and mail your complaint form
[Body]     Print, sign, and mail to: California Privacy Protection Agency,
           ATTN: Complaints, 400 R Street, Suite 350, Sacramento, CA 95811.
[Link]     Download CPPA Complaint PDF ↓
```

- Same typography pattern as Card A
- Link: `font-mono text-[11px] uppercase tracking-[0.1em] border border-border rounded-[6px] px-3 py-1 text-text-mid hover:text-text` (secondary/ghost style — matches existing PDF download link pattern)
- href: `/api/filings/${filing.id}/cppa-pdf`
- Condition: rendered only when `filing.category !== 'accessibility'`

**Channel Card C — CA AG Auto-Filed (always shown)**

Step label is dynamic:
- Non-ADA: `STEP 3 — CA Attorney General`
- ADA: `STEP 1 — CA Attorney General`

```
[Eyebrow]  STEP {N} — CA Attorney General               [Badge: Auto-Filed ✓]
[Heading]  Your complaint was automatically submitted by fax
[Fax row]  Fax ID: {faxId}   (omit row entirely if faxId is null)
[Status]   Status: {status label}
[Link]     Download Your Complaint PDF ↓   (only if complaintPdfUrl set)
```

- "Auto-Filed ✓" badge: same style as "Paid" badge (`bg-bg-dark text-white`)
- Fax ID row: `font-mono text-[11px] uppercase tracking-[0.1em] text-text-light` label +
  `font-mono text-sm text-text` value. Omit the entire row when `faxId` is null.
- Status label: see Fax Status States table below
- PDF link: same ghost style as Card B

**Fax Status States**

| `faxStatus` DB value | Display label | Text color token |
|----------------------|--------------|------------------|
| `'success'` | Delivered | `text-accent-green` |
| `'failure'` or `'partialsuccess'` | Delivery Failed | `text-accent` (red) |
| `'queued'` | Pending | `text-text-light` |
| `'inprogress'` | Pending | `text-text-light` |
| `null` | Pending | `text-text-light` |

Status display pattern:
```
Status: <span class="font-mono text-[11px] uppercase px-2 py-0.5 rounded-[4px] bg-{color}-50 text-{color}">
  {label}
</span>
```
Use `bg-accent-bg text-accent` for "Delivery Failed", plain `text-text-light` inline for
"Pending", and `text-accent-green` inline for "Delivered". Do not add a colored background
to "Pending" — it is a non-event state.

### Section 5: Guest Account CTA (unchanged — preserve exactly)

No changes to layout or copy. Already implements SUCC-04. Preserve the card at the same
position below the three channel cards. Verified: `!filing.userId` condition.

### Section 6: Secondary Action (unchanged)

"File Another Complaint" link-button. Preserve exactly.

---

## ADA Conditional Logic

`const isADA = filing.category === 'accessibility'`

When `isADA` is true:
- Channel Card A (CPPA Online) is NOT rendered
- Channel Card B (CPPA Paper PDF) is NOT rendered
- Channel Card C step label reads "STEP 1 — CA Attorney General"
- Section 3 "Agency" detail row still reads "CA Attorney General"
- Confirmation subheading copy changes (see Copywriting Contract)

When `isADA` is false:
- All three channel cards are rendered
- Channel Card C step label reads "STEP 3 — CA Attorney General"
- Section 3 "Agency" detail row reads "Multiple Agencies"

Implementation: no JS/CSS toggle. Conditional rendering via JSX `{!isADA && (...)}`.
ADA sections are never in the DOM for ADA filers — they are not hidden with `display:none`
or `aria-hidden`. This is required for accessibility correctness (hidden content is still
discoverable by assistive technology).

---

## Copywriting Contract

All copy is for the success page only. Backend deliverables have no UI copy surface.

| Element | Non-ADA copy | ADA copy |
|---------|-------------|----------|
| Confirmation subheading | "Your privacy complaint has been submitted. Follow the steps below to complete all three filing channels." | "Your accessibility complaint has been submitted to the California Attorney General." |
| Section 3 Agency row | Multiple Agencies | CA Attorney General |
| Channel Card A eyebrow | STEP 1 — CPPA Online | (not rendered) |
| Channel Card A heading | File your complaint online at cppa.ca.gov | (not rendered) |
| Channel Card A body | We've prepared your answers. Open our step-by-step guide, then paste each answer into the CPPA form. | (not rendered) |
| Channel Card A CTA | File Now — Step-by-Step Guide → | (not rendered) |
| Channel Card B eyebrow | STEP 2 — CPPA Paper Mail | (not rendered) |
| Channel Card B heading | Download and mail your complaint form | (not rendered) |
| Channel Card B body | Print, sign, and mail to: California Privacy Protection Agency, ATTN: Complaints, 400 R Street, Suite 350, Sacramento, CA 95811. | (not rendered) |
| Channel Card B CTA | Download CPPA Complaint PDF ↓ | (not rendered) |
| Channel Card C eyebrow | STEP 3 — CA Attorney General | STEP 1 — CA Attorney General |
| Channel Card C heading | Your complaint was automatically submitted by fax | Your complaint was automatically submitted by fax |
| Channel Card C "Auto-Filed" badge | Auto-Filed ✓ | Auto-Filed ✓ |
| Fax status — delivered | Delivered | Delivered |
| Fax status — failed | Delivery Failed | Delivery Failed |
| Fax status — pending/null | Pending | Pending |
| Guest CTA heading | Track Your Filings | Track Your Filings |
| Guest CTA body | Create a free account to view your filing history and download your complaint PDF anytime. | Create a free account to view your filing history and download your complaint PDF anytime. |
| Guest CTA button | Create Free Account → | Create Free Account → |
| Secondary action | File Another Complaint | File Another Complaint |
| Not found heading | Filing Not Found | Filing Not Found |
| Not found body | This filing receipt may have expired. Start a new complaint from the home page. | This filing receipt may have expired. Start a new complaint from the home page. |

**Destructive actions in this phase:** None. No delete, cancel, or irreversible action
exists on the success page. No confirmation dialogs needed.

**Empty state:** The not-found state is already implemented. Preserve existing copy
exactly — no changes needed.

**Error state:** No new error state is introduced. Fax "Delivery Failed" is a status
display, not an error state. It does not require a recovery action from this page.

---

## Interaction Contracts

| Interaction | Behavior | Notes |
|-------------|----------|-------|
| Channel Card A link | Opens `/filing/[id]/cppa-guide` — same tab | Internal navigation; no `target="_blank"` |
| Channel Card B link | Opens `/api/filings/[id]/cppa-pdf` — triggers file download | Browser handles download via Content-Disposition header |
| Channel Card C PDF link | Opens `/api/filings/[id]/pdf` — triggers file download | Same pattern as existing PDF proxy |
| Guest CTA button | Navigates to `/account/create?filingId={id}` — same tab | Existing href pattern; preserve |
| File Another Complaint | Navigates to `/file` | Existing href; preserve |

No hover states beyond what Tailwind `hover:` utilities already provide.
No loading states on this page — it is a server-rendered component with no client-side
data fetching. No skeleton loaders needed.
No polling on fax status. The server renders whatever `faxStatus` is at page load.
A consumer who wants the latest fax status must refresh the page manually.

---

## Accessibility Requirements

- All channel cards must be navigable via keyboard Tab in document order.
- The `✓` glyph in the confirmation header retains `aria-hidden="true"` (existing).
- The receipt ID uses `<output aria-label="Filing receipt ID: {id}">` (existing — preserve).
- Fax status text must not rely on color alone: always include the text label ("Delivered",
  "Delivery Failed", "Pending") alongside the color — never a color-only indicator.
- Channel Card A, B, and C headings (`font-serif text-xl font-bold`) must be rendered
  as `<h2>` elements (not `<p>` or `<div>`) to establish landmark hierarchy under the `<h1>`.
- The "Auto-Filed ✓" checkmark character in the badge is decorative; wrap in
  `aria-hidden="true"` span if needed so screen readers don't announce "check mark".
- Links with "→" arrow suffix: the arrow is presentational. Use `aria-label` on each link
  if the link text alone is ambiguous out of context. Preferred: make link text self-
  describing ("File Now — Step-by-Step Guide" is sufficient without aria-label).

---

## Layout

Page wrapper: `min-h-screen bg-bg` with `<Masthead />` sticky header and `<Footer />`.
Content column: `max-w-3xl mx-auto px-6 py-12` — unchanged from current page.

The three channel cards are vertically stacked within the content column.
No grid or flex multi-column layout. Single column on all breakpoints.
No horizontal scrolling. No fixed-height containers.

Page width on mobile: `px-6` (24px) side padding — same as current.
Cards: full width of content column (`w-full` implied).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
