---
phase: 07-landing-page-and-legal-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/HomeFaq.tsx
  - src/components/Masthead.tsx
  - src/components/Footer.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [MKTG-01, MKTG-02, MKTG-03]

must_haves:
  truths:
    - "Homepage hero displays 'File a Privacy Complaint in 5 Minutes' as h1"
    - "Primary CTA links to /file with text 'File a Privacy Complaint'"
    - "How It Works section shows exactly 3 steps, not 4"
    - "FAQ section shows 5 collapsible questions using @base-ui/react Accordion"
    - "Stats sidebar shows $1.99 per filing, not $0.50"
    - "Masthead Sign In links to /login, not /auth/signin"
    - "Footer legal links point to /privacy and /terms, not href='#'"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Rewritten homepage with current product copy"
      contains: "File a Privacy Complaint in 5 Minutes"
    - path: "src/components/HomeFaq.tsx"
      provides: "Client-side FAQ accordion with 5 questions"
      contains: "use client"
    - path: "src/components/Masthead.tsx"
      provides: "Updated nav with /login href"
      contains: "/login"
    - path: "src/components/Footer.tsx"
      provides: "Updated footer with real legal links and CA AG agency"
      contains: "/privacy"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/HomeFaq.tsx"
      via: "import { HomeFaq }"
      pattern: "import.*HomeFaq"
    - from: "src/app/page.tsx"
      to: "/file"
      via: "Link href"
      pattern: 'href="/file"'
---

<objective>
Rewrite the homepage to match the current $1.99 single-agency privacy filing product, add a collapsible FAQ section, and update Masthead/Footer navigation links.

Purpose: The existing homepage was built for an old multi-agency $0.50+$2/year model. Every section needs copy updates, the categories grid needs replacement, and nav links need fixing.
Output: Updated page.tsx, new HomeFaq.tsx, updated Masthead.tsx, updated Footer.tsx, updated layout.tsx metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-landing-page-and-legal-pages/07-RESEARCH.md
@.planning/phases/07-landing-page-and-legal-pages/07-UI-SPEC.md

@src/app/page.tsx
@src/components/Masthead.tsx
@src/components/Footer.tsx
@src/components/DoubleRule.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite homepage copy, categories, steps, pricing, and update layout metadata</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/page.tsx (current homepage — full rewrite needed)
    - src/app/layout.tsx (metadata needs update)
    - src/components/DoubleRule.tsx (reused component — keep imports)
    - .planning/phases/07-landing-page-and-legal-pages/07-UI-SPEC.md (design contract)
  </read_first>
  <action>
**page.tsx — Full rewrite of all content sections:**

1. **Hero section:** Keep the grid layout structure (`grid grid-cols-1 lg:grid-cols-3`). Replace content:
   - Eyebrow: change from "Consumer Rights · Special Report" to "Privacy Rights · Filing Service"
   - h1: `"File a Privacy Complaint in 5 Minutes"` with the em tag: `<em className="text-accent">in 5 Minutes</em>` (per MKTG-01)
   - Body paragraph 1: "California businesses are required to respect your privacy rights under the CCPA. When they don't, you have the right to file a formal complaint with the California Attorney General. EasyFilerComplaint handles the paperwork."
   - Body paragraph 2: "Tell us what happened. We generate a formal complaint letter, fax it directly to the CA Attorney General's office, and email you a copy with your unique filing receipt ID. The whole process takes about 5 minutes."
   - Primary CTA: `href="/file"`, text: `"File a Privacy Complaint →"` (per MKTG-01, UI-SPEC)
   - Secondary CTA: keep `href="#how-it-works"`, text: `"See How It Works"` (unchanged)

2. **Stats sidebar:** Replace the stats array with exactly these 4 items:
   ```
   { stat: '$1.99', label: 'Per Filing' },
   { stat: '5 min', label: 'Average Time' },
   { stat: '1', label: 'Government Agency' },
   { stat: '3', label: 'Complaint Types' },
   ```
   Keep the disclaimer text: "Not a law firm. Filing assistance only. Results not guaranteed."

3. **Categories section:** Replace the old 6-category `categories` array and section with a 3-card "What You Can File" section. New data array named `complaintTypes`:
   ```typescript
   const complaintTypes = [
     {
       symbol: '§',
       title: 'Privacy Tracking Violations',
       description: 'Unauthorized data collection, sale of personal information, CCPA violations, failure to honor opt-out requests.',
       agency: 'CA Attorney General',
     },
     {
       symbol: '¶',
       title: 'Website Accessibility Barriers',
       description: 'Websites or apps that are inaccessible to people with disabilities, violating accessibility requirements.',
       agency: 'CA Attorney General',
     },
     {
       symbol: '†',
       title: 'Video Sharing Privacy',
       description: 'Unauthorized sharing or distribution of personal videos, violation of video privacy protections.',
       agency: 'CA Attorney General',
     },
   ]
   ```
   Each card renders: symbol (font-serif text-4xl), title (font-serif text-lg font-bold), description, single agency badge showing "CA Attorney General". Price badge on each card: `$1.99`. Section heading: "What Can You File?"

4. **How It Works section:** Replace 4-step `steps` array with 3 steps. Change section heading from "Four Steps to an Official Filing" to "Three Steps to an Official Filing". Grid changes from `lg:grid-cols-4` to `lg:grid-cols-3`. New steps array:
   ```typescript
   const steps = [
     { numeral: 'I', title: 'Describe the Problem', description: 'Tell us what happened — the business name, what privacy violation occurred, and your contact details.' },
     { numeral: 'II', title: 'We Generate & File', description: 'We create a formal complaint letter and fax it directly to the California Attorney General\'s office.' },
     { numeral: 'III', title: 'Get Your Receipt', description: 'You receive a confirmation email with your complaint PDF and a unique filing receipt ID.' },
   ]
   ```

5. **Pricing section:** Replace subscription model with flat fee. Remove "Annual Membership" label. New content:
   - Eyebrow: keep "Rates"
   - Heading inside card: "Simple Pricing" (font-mono text-[9px] label style)
   - Price: `$1.99` with "per filing" suffix (not "per annum")
   - Sub-price text: "Flat fee. No subscription. No surprise charges." (remove the old `$0.50 per filing` accent text)
   - Feature list (keep § bullet style):
     - "Formal complaint letter generated"
     - "Faxed to the California Attorney General"
     - "PDF copy emailed to you"
     - "Unique filing receipt ID"
     - "Account to track your filings"
   - CTA button: `href="/file"`, text: `"File a Complaint — $1.99 →"` (not `/auth/signin`)
   - Footer text: "No account required to file · Create one after to track filings"

6. **FAQ section:** Add a new section AFTER pricing and BEFORE Footer. Structure:
   ```tsx
   {/* FAQ */}
   <section id="faq" className="border-b border-border">
     <div className="max-w-6xl mx-auto px-6 py-12">
       <div className="flex items-center gap-4 mb-8">
         <span className="font-mono text-[11px] tracking-[0.1em] uppercase text-text-light">Support</span>
         <div className="flex-1 border-t border-border" />
       </div>
       <h2 className="font-serif text-3xl font-bold text-text mb-10">
         Common Questions
       </h2>
       <div className="max-w-3xl mx-auto">
         <HomeFaq />
       </div>
     </div>
   </section>
   ```
   Add import at top: `import { HomeFaq } from '@/components/HomeFaq'`

7. **Remove:** Delete the old `categories` const array (lines 6-43). Delete the old `steps` const array (lines 45-50).

**layout.tsx — Update metadata only:**
Change the metadata export to:
```typescript
export const metadata: Metadata = {
  title: 'EasyFilerComplaint — File a Privacy Complaint in 5 Minutes',
  description: 'File a formal privacy complaint with the California Attorney General for $1.99. We generate the paperwork, fax it to the AG, and email you a copy.',
}
```
Do NOT change anything else in layout.tsx (fonts, RootLayout component, etc.).
  </action>
  <verify>
    <automated>rtk vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains the string "File a Privacy Complaint in 5 Minutes"
    - page.tsx contains `href="/file"` (at least 2 occurrences — hero CTA and pricing CTA)
    - page.tsx contains "Three Steps to an Official Filing"
    - page.tsx contains `lg:grid-cols-3` for both complaint types grid and steps grid
    - page.tsx does NOT contain "$0.50" or "Annual Membership" or "/auth/signin" or "Seven government agencies" or "FCC, FTC"
    - page.tsx contains `import { HomeFaq }` from '@/components/HomeFaq'
    - page.tsx contains `id="faq"` section
    - page.tsx contains the string "$1.99" (stats sidebar + pricing section)
    - layout.tsx metadata title contains "File a Privacy Complaint in 5 Minutes"
    - layout.tsx metadata description contains "California Attorney General"
    - layout.tsx metadata description does NOT contain "FCC" or "FTC" or "CFPB" or "FDA" or "EPA"
    - All 130 existing tests still pass
  </acceptance_criteria>
  <done>Homepage displays current product ($1.99, single agency, 3 complaint types, 3 steps) with working CTA to /file, FAQ section placeholder, and layout metadata updated.</done>
</task>

<task type="auto">
  <name>Task 2: Create HomeFaq client component with @base-ui/react Accordion</name>
  <files>src/components/HomeFaq.tsx</files>
  <read_first>
    - .planning/phases/07-landing-page-and-legal-pages/07-UI-SPEC.md (accordion pattern, trigger/panel styling, FAQ copy)
    - .planning/phases/07-landing-page-and-legal-pages/07-RESEARCH.md (Pattern 2: Client Component FAQ, Pitfall 1 and 3)
  </read_first>
  <action>
Create `src/components/HomeFaq.tsx` as a `'use client'` component.

Import `{ Accordion }` from `'@base-ui-components/react/accordion'` (NOTE: the actual import path for @base-ui/react parts — verify from node_modules if this path fails; alternative is `@base-ui/react/accordion`).

Also import `{ ChevronDown }` from `'lucide-react'` for the expand/collapse indicator.

**FAQ_ITEMS array** (exactly 5 items, verbatim from UI-SPEC copywriting contract):

```typescript
const FAQ_ITEMS = [
  {
    id: 'q1',
    question: 'What happens after I file?',
    answer: "Your complaint is faxed directly to the California Attorney General's office. You receive a copy of your complaint PDF by email along with a unique filing receipt ID. Government agencies handle complaints on their own timelines.",
  },
  {
    id: 'q2',
    question: 'Is EasyFilerComplaint a law firm?',
    answer: 'No. EasyFilerComplaint is a complaint filing service. We prepare and transmit your complaint document to the appropriate government office. We do not provide legal advice and no attorney-client relationship is created.',
  },
  {
    id: 'q3',
    question: 'What types of privacy complaints can I file?',
    answer: 'Currently we support three types: privacy tracking violations (CCPA), website accessibility barriers, and video sharing privacy violations. All complaints are submitted to the California Attorney General.',
  },
  {
    id: 'q4',
    question: 'Will I get a refund if my complaint is rejected?',
    answer: 'The $1.99 fee covers the filing service itself — preparing and transmitting your complaint. Agency decisions about whether to act on a complaint are outside our control. Fees are non-refundable after submission.',
  },
  {
    id: 'q5',
    question: 'How do I know my complaint was filed?',
    answer: 'You receive a filing receipt email at the address you provided. The email includes your unique receipt ID (formatted EFC-YYYYMMDD-XXXXX), a copy of your complaint PDF, and confirmation that it was transmitted to the CA AG.',
  },
]
```

**Component structure:**

```tsx
export function HomeFaq() {
  return (
    <Accordion.Root>
      {FAQ_ITEMS.map((item) => (
        <Accordion.Item key={item.id} value={item.id} className="border-b border-border">
          <Accordion.Header>
            <Accordion.Trigger className="font-body text-base font-medium text-text w-full text-left py-4 flex items-center justify-between hover:text-text-mid transition-colors group">
              {item.question}
              <ChevronDown className="w-4 h-4 text-text-light transition-transform duration-200 group-data-[state=open]:rotate-180" />
            </Accordion.Trigger>
          </Accordion.Header>
          <Accordion.Panel className="font-body text-base text-text-mid leading-relaxed pb-4">
            {item.answer}
          </Accordion.Panel>
        </Accordion.Item>
      ))}
    </Accordion.Root>
  )
}
```

**Export the FAQ_ITEMS array** as a named export so tests can verify content without rendering: `export { FAQ_ITEMS }`.

**CRITICAL:** The file MUST start with `'use client'` directive. @base-ui/react Accordion uses React hooks internally and will fail in a Server Component context.
  </action>
  <verify>
    <automated>rtk vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/HomeFaq.tsx exists
    - HomeFaq.tsx first line is `'use client'`
    - HomeFaq.tsx contains `export function HomeFaq`
    - HomeFaq.tsx contains `export { FAQ_ITEMS }` or `export const FAQ_ITEMS`
    - HomeFaq.tsx contains exactly 5 objects in FAQ_ITEMS array (ids q1 through q5)
    - HomeFaq.tsx contains `Accordion.Root`, `Accordion.Item`, `Accordion.Trigger`, `Accordion.Panel`
    - HomeFaq.tsx contains `ChevronDown` import from lucide-react
    - HomeFaq.tsx does NOT contain any of: DPW, Pro Veritas, APFC, ComplianceSweep, IdentifiedVerified
    - All existing tests still pass
  </acceptance_criteria>
  <done>HomeFaq component renders 5 collapsible FAQ items using @base-ui/react Accordion with chevron indicators and exported FAQ_ITEMS array for testing.</done>
</task>

<task type="auto">
  <name>Task 3: Update Masthead and Footer navigation links</name>
  <files>src/components/Masthead.tsx, src/components/Footer.tsx</files>
  <read_first>
    - src/components/Masthead.tsx (current nav links — /auth/signin needs to become /login)
    - src/components/Footer.tsx (dead href="#" links, stale agencies list)
    - .planning/phases/07-landing-page-and-legal-pages/07-UI-SPEC.md (component inventory, footer link targets)
  </read_first>
  <action>
**Masthead.tsx changes:**
1. Change "Sign In" link href from `"/auth/signin"` to `"/login"` (Phase 6 canonical login page). Keep all class names and text identical.

**Footer.tsx changes:**
1. **Platform column:** Update the href values in the Platform links map:
   - "File a Complaint" → `href="/file"`
   - "How It Works" → `href="/#how-it-works"`
   - "Pricing" → `href="/#pricing"`
   - "Dashboard" → `href="/account/filings"`
   Change from `.map` with generic `href="#"` to explicit link objects:
   ```typescript
   const platformLinks = [
     { label: 'File a Complaint', href: '/file' },
     { label: 'How It Works', href: '/#how-it-works' },
     { label: 'Pricing', href: '/#pricing' },
     { label: 'Dashboard', href: '/account/filings' },
   ]
   ```

2. **Legal column:** Replace dead `href="#"` links. Remove "Refund Policy" (no requirement for standalone refund page — refund terms are in /terms). Wire remaining links:
   ```typescript
   const legalLinks = [
     { label: 'Privacy Policy', href: '/privacy' },
     { label: 'Terms of Service', href: '/terms' },
     { label: 'About', href: '/about' },
   ]
   ```

3. **Agencies column:** Replace `['FCC', 'FTC', 'CFPB', 'FDA', 'EPA', 'DOJ/ADA']` with `['CA Attorney General']` (single active agency in v1).

Keep all existing className values, the brand logo section, and the copyright footer line.
  </action>
  <verify>
    <automated>rtk vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Masthead.tsx contains `href="/login"` and does NOT contain `href="/auth/signin"`
    - Footer.tsx contains `href="/privacy"` and `href="/terms"` and `href="/about"`
    - Footer.tsx does NOT contain `href="#"` (no dead links remain)
    - Footer.tsx contains "CA Attorney General" in the agencies section
    - Footer.tsx does NOT contain "FCC" or "FTC" or "CFPB" or "FDA" or "EPA" or "DOJ/ADA"
    - Footer.tsx does NOT contain "Refund Policy"
    - Footer.tsx contains `href="/file"` for "File a Complaint" link
    - Footer.tsx contains `href="/account/filings"` for "Dashboard" link
    - All existing tests still pass
  </acceptance_criteria>
  <done>Masthead Sign In links to /login, Footer has real links to /privacy, /terms, /about, /file, shows CA Attorney General only, no dead links.</done>
</task>

</tasks>

<verification>
- `rtk vitest run` — all 130+ existing tests pass
- `grep -r '/auth/signin' src/app/page.tsx src/components/Masthead.tsx` returns no results
- `grep -r 'href="#"' src/components/Footer.tsx` returns no results
- `grep 'File a Privacy Complaint in 5 Minutes' src/app/page.tsx` returns a match
- `grep 'HomeFaq' src/app/page.tsx` returns the import line
</verification>

<success_criteria>
Homepage hero says "File a Privacy Complaint in 5 Minutes", CTAs link to /file, 3 complaint types shown, 3 steps in How It Works, FAQ section with 5 collapsible questions, pricing shows $1.99 flat fee, Masthead links to /login, Footer links to real legal pages and shows CA AG only.
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-and-legal-pages/07-01-SUMMARY.md`
</output>
